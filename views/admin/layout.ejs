<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Admin Dashboard - StorehStockHub' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/admin.css" rel="stylesheet">
    <style>
        .admin-sidebar {
            min-height: 100vh;
            background: #343a40;
            color: white;
        }
        .admin-sidebar .nav-link {
            color: rgba(255,255,255,.75);
            padding: 0.75rem 1rem;
        }
        .admin-sidebar .nav-link:hover,
        .admin-sidebar .nav-link.active {
            color: white;
            background: rgba(255,255,255,.1);
        }
        .admin-content {
            margin-left: 0;
            transition: margin-left 0.3s;
        }
        @media (min-width: 768px) {
            .admin-content {
                margin-left: 250px;
            }
        }
        .admin-header {
            background: #fff;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="admin-sidebar position-fixed d-flex flex-column p-0" style="width: 250px; top: 0; left: 0; z-index: 1000;">
        <div class="d-flex align-items-center px-3 py-3 border-bottom border-secondary">
            <i class="fas fa-store fa-2x me-3"></i>
            <h5 class="mb-0">Admin Panel</h5>
        </div>

        <ul class="nav nav-pills flex-column px-2 py-3">
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'dashboard' ? 'active' : '' %>" href="/admin">
                    <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'products' ? 'active' : '' %>" href="/admin/products">
                    <i class="fas fa-box me-2"></i>Products
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'orders' ? 'active' : '' %>" href="/admin/orders">
                    <i class="fas fa-shopping-cart me-2"></i>Orders
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'stock' ? 'active' : '' %>" href="/admin/stock">
                    <i class="fas fa-warehouse me-2"></i>Stock Management
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'files' ? 'active' : '' %>" href="/admin/files">
                    <i class="fas fa-file-upload me-2"></i>Files
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'logs' ? 'active' : '' %>" href="/admin/logs">
                    <i class="fas fa-history me-2"></i>Activity Logs
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <%= currentPage === 'settings' ? 'active' : '' %>" href="/admin/settings">
                    <i class="fas fa-cog me-2"></i>Settings
                </a>
            </li>
        </ul>

        <div class="mt-auto px-3 py-3 border-top border-secondary">
            <a href="/" class="text-decoration-none text-white">
                <i class="fas fa-store me-2"></i>Back to Store
            </a>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="admin-content">
        <!-- Header -->
        <header class="admin-header">
            <div class="container-fluid">
                <div class="d-flex justify-content-between align-items-center py-3 px-4">
                    <div>
                        <h4 class="mb-0"><%= title || 'Dashboard' %></h4>
                        <small class="text-muted">Welcome back, <%= user || 'Admin' %></small>
                    </div>
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <i class="fas fa-bell text-muted"></i>
                        </div>
                        <form action="/logout" method="POST" class="d-inline">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <button type="submit" class="btn btn-outline-danger btn-sm">
                                <i class="fas fa-sign-out-alt me-1"></i>Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </header>

        <!-- Page Content -->
        <main class="container-fluid px-4 pb-4">
            <%- body %>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin.js"></script>
</body>
</html>
